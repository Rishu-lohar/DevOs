import { GlassCard } from "../ui";
import { chartData } from "@/lib/mock-data";
import { TrendingUp } from "lucide-react";

export function AreaChart() {
  const points = chartData.map((value, index) => `${index * 9},${100 - value}`).join(" ");
  return <GlassCard className="p-5 sm:p-6"><div className="mb-6 flex items-start justify-between"><div><div className="flex items-center gap-2"><h3 className="text-sm font-semibold tracking-tight text-white">Productivity Velocity</h3><span className="inline-flex items-center gap-1 rounded-full border border-emerald-400/25 bg-emerald-500/12 px-2 py-0.5 text-[11px] font-medium text-emerald-300"><TrendingUp size={11} />+28.4%</span></div><p className="mt-1 text-xs text-slate-500">Velocity trajectory across the last 12 engineering sprints</p></div><div className="flex items-center gap-1.5 rounded-lg border border-white/[.07] bg-white/[.03] px-2.5 py-1 text-[11px] text-slate-400"><span className="h-2 w-2 rounded-full bg-violet-400" /><span>Output Rate</span></div></div><div className="relative h-44 w-full"><div className="absolute inset-0 flex flex-col justify-between opacity-15 pointer-events-none">{Array.from({ length: 4 }).map((_, index) => <div key={index} className="w-full border-b border-dashed border-white/30" />)}</div><svg viewBox="0 0 100 100" className="h-full w-full overflow-visible" preserveAspectRatio="none"><defs><linearGradient id="areaGradient" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#a78bfa" stopOpacity="0.4" /><stop offset="65%" stopColor="#7c3aed" stopOpacity="0.1" /><stop offset="100%" stopColor="#7c3aed" stopOpacity="0" /></linearGradient></defs><polygon points={`0,100 ${points} 100,100`} fill="url(#areaGradient)" /><polyline points={points} fill="none" stroke="#c4b5fd" strokeWidth="2" vectorEffect="non-scaling-stroke" /></svg></div><div className="mt-3 flex justify-between text-[10px] font-medium text-slate-600"><span>Week 1</span><span>Week 4</span><span>Week 8</span><span>Week 12</span></div></GlassCard>;
}
